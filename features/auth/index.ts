/**
 * Auth Feature
 * Public API exports for authentication functionality
 */

// Components
export { default as AuthModal } from '../../components/AuthModal';
export { default as AuthProvider } from '../../components/AuthProvider';

// Hooks
export { useAuthStore } from '../../lib/store';

// Types
export type { User } from '../../lib/types';

